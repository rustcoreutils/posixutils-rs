dnl for(iter, start, end, output)
define(`for', `pushdef(`$1', `$2')_for($@)popdef(`$1')')
define(`_for', `$4`'ifelse($1, `$3', `', `define(`$1', incr($1))$0($@)')')
for(`i', `1', `8', `i ')
for(`i', `1', `4', `for(`j', `1', `8', ` (i, j)')
')
